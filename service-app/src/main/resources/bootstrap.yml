spring:
  application:
    name: service-app-bootstrap
  cloud:
    config:
#      uri: ${SPRING_CONFIG_URI:http://localhost:8888}
      fail-fast: true
      discovery:
        enabled: true
        service-id: config-server
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVICE_URL:http://huzd.me:8761}/eureka
